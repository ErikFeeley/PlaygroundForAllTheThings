using MediatR;
using StructureMap;
using StructureMap.Configuration.DSL.Expressions;

namespace MediatrEF6PoC3.API.Extensions

    // alright shit maybe this wont work

{
    //public static class StructureMapConfigExtensions
    //{
    //    public static CreatePluginFamilyExpression<T> ConfigureMediatrStuff<T>(
    //        this ConfigurationExpression configExpression)
    //    {
    //        configExpression.For<SingleInstanceFactory>().Use<SingleInstanceFactory>(ctx => t => ctx.GetInstance(t));
    //        configExpression.For<MultiInstanceFactory>().Use<MultiInstanceFactory>(ctx => t => ctx.GetAllInstances(t));
    //        configExpression.For<IMediator>().Use<Mediator>();
    //    }
    //}
}
